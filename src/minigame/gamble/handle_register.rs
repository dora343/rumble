use serenity::all::UserId;

use serenity::utils::MessageBuilder;

pub async fn handle_register(user_id: UserId) -> String {
    // lookup user_id
    // early return if found
    
    
    
    
    String::from("")
}